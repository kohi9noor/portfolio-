import { MoonIcon } from "lucide-react";

const DarkModeToggle = () => (
  <div className="flex items-center justify-center w-12 cursor-pointer h-12 rounded-full hover:bg-slate-200">
    <MoonIcon size={18} />
  </div>
);

export default DarkModeToggle;
